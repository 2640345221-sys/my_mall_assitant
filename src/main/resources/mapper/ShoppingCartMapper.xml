<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="assitant.mapper.ShoppingCartMapper">

    <select id="findByUserIdWithGoods" resultType="assitant.entity.dto.CartItemVO">
        select
            c.id          as cartId,
            c.user_id     as userId,
            c.goods_id    as goodsId,
            c.goods_count as goodsCount,
            g.name        as goodsName,
            g.cover_img   as coverImg,
            g.selling_price as sellingPrice
        from my_mall.shopping_cart c
        join my_mall.goods g on g.id = c.goods_id
        where c.user_id = #{userId}
    </select>

</mapper>