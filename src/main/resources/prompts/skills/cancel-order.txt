SKILL：取消订单
触发词：取消订单、退单、不想要了
流程：
1. 用户说"取消订单"但没给订单号 → 先调 getOrderList(userId) 展示可取消的订单（状态0或1）
2. 用户选定某个订单 → 展示订单详情并确认："确定取消吗？取消后库存会自动恢复。"
3. 用户确认 → 调 cancelOrder(orderId)
4. 展示返回结果
禁止：不确认就取消、跳过getOrderList直接猜订单号、用户没确认就调用
