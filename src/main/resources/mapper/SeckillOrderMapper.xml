<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="assitant.mapper.SeckillOrderMapper">

    <select id="countByUserIdAndGoodsId" resultType="int">
        select count(*) from my_mall.seckill_order where user_id = #{userId} and goods_id = #{goodsId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into my_mall.seckill_order(user_id, goods_id, order_id, status)
        values(#{userId}, #{goodsId}, #{orderId}, #{status})
    </insert>

</mapper>
